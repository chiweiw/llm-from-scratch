# 简易发包工具桌面版 - 需求文档

## 项目信息

- **项目名称**: 简易发包工具桌面版 (DeployTool Desktop)
- **技术栈**: Wails + Go + Vue.js
- **版本号**: v2.0
- **日期**: 2026-02-28
- **状态**: 需求分析

---

## 1. 项目背景与目标

### 1.1 背景

现有 Python 脚本 `deploy_tool.py` 实现了本地 Maven 打包 + 多服务器上传 + 远程重启的自动化部署流程，但存在以下问题：

- 命令行界面不够直观
- 配置修改需要编辑代码文件
- 缺少可视化的进度反馈
- 无法保存多套环境配置
- 日志查看不便

### 1.2 核心目标

开发一个基于 Wails + Go 的 Windows 桌面应用，实现：

- 可视化配置管理（支持多套环境）
- 实时进度展示与日志输出
- 一键部署流程
- 配置自检与错误提示
- 部署历史记录

---

## 2. 功能需求 (Functional Requirements)

### 2.1 环境管理模块

#### 2.1.1 环境配置

支持多套环境配置，每套环境包含：

**基本信息**:
- 环境名称（如：开发环境、测试环境、生产环境）
- 环境标识（如：dev、test、prod）
- 环境描述（可选）

**本地环境配置**:
- 项目根目录（包含 pom.xml 的目录）
- JDK 安装路径
- Maven 可执行文件路径
- Maven 配置文件路径（settings.xml）
- Maven 本地仓库路径
- Maven 命令参数（可自定义）
- 是否使用安静模式
- 是否精简日志
- 是否显式指定 pom 文件
- 是否离线构建

**服务器配置**（每个环境支持 1 到多个服务器）:
- 服务器名称（如：主服务器、备份服务器）
- 主机地址
- SSH 端口
- 登录用户名
- 登录密码（加密存储）
- 远程部署目录
- 重启脚本路径
- 是否启用重启
- 是否使用 sudo
- **改名规则**（不同环境可配置不同的改名规则）
  - 例如：`{jar_name}_{env}_{timestamp}.jar`
  - 例如：`{jar_name}.jar`（不改名）
  - 支持变量：`{jar_name}`, `{env}`, `{timestamp}`, `{date}`, `{version}`

**目标文件配置**（支持多个 Jar）:
- 本地 Jar 相对路径
- 远程部署文件名（可选，如不设置则使用改名规则）
- 是否默认选中（部署时是否默认勾选）

#### 2.1.2 环境模板

提供预初始化的环境模板，用户可以选择模板快速创建：

**开发环境模板** (Dev):
```
- 环境名称: 开发环境
- 改名规则: {jar_name}.jar (不改名)
- Maven 设置: 离线构建、精简日志
- 服务器: 通常 1 台开发服务器
- 重启脚本: restart_jar_dev.sh
```

**测试环境模板** (Test):
```
- 环境名称: 测试环境
- 改名规则: {jar_name}_test.jar
- Maven 设置: 在线构建、完整日志
- 服务器: 通常 1-2 台测试服务器
- 重启脚本: restart_jar_test.sh
```

**生产环境模板** (Prod):
```
- 环境名称: 生产环境
- 改名规则: {jar_name}_{timestamp}.jar
- Maven 设置: 在线构建、完整日志
- 服务器: 通常 2+ 台生产服务器（集群）
- 重启脚本: restart_jar_prod.sh
- 特殊提示: 部署前需二次确认
```

用户可以：
- 选择模板快速创建环境（自动填充配置）
- 基于模板自定义修改
- 从零开始自定义创建
- 复制现有环境配置
- 删除不需要的环境

#### 2.1.3 Jar 包选择

部署时支持灵活选择要发送的 Jar 包：

**选择方式**:
- 默认全部选中
- 可单独勾选/取消勾选每个 Jar 包
- 支持全选/全不选
- 记住上次选择（可选）

**选择界面**:
```
📦 选择要部署的文件:
☑ platform-startup-project.jar (默认选中)
☑ platform-startup-system.jar (默认选中)
☐ platform-startup-customer.jar (默认不选中)

[全选] [全不选] [使用上次选择]
```

**改名规则应用**:
- 每个选中的 Jar 包按照环境的改名规则重命名
- 不同环境可以有不同的改名规则
- 改名规则示例：
  - 开发环境: `platform-startup-project.jar` → `platform-startup-project.jar`
  - 测试环境: `platform-startup-project.jar` → `platform-startup-project-test.jar`
  - 生产环境: `platform-startup-project.jar` → `platform-startup-project_20260228_143025.jar`

### 2.2 配置自检模块

#### 2.2.1 本地环境自检

在部署前自动检测：

- [x] 项目根目录是否存在
- [x] pom.xml 是否存在
- [x] JDK 是否安装（javac 可执行）
- [x] JAVA_HOME 是否正确配置
- [x] Maven 是否可用（mvn 命令）
- [x] Maven settings 文件是否存在
- [x] Maven 本地仓库路径是否可用
- [x] 目标 Jar 文件是否存在（打包后）

#### 2.2.2 远程环境自检

在部署前自动检测：

- [x] 服务器网络连通性（ping）
- [x] SSH 端口是否可达
- [x] SSH 登录是否成功
- [x] 远程目录是否存在且可写
- [x] 重启脚本是否存在且可执行
- [x] sudo 权限检查（如启用）

#### 2.2.3 自检结果展示

- ✅ 绿色图标：检查通过
- ❌ 红色图标：检查失败
- ⚠️ 黄色图标：警告（可继续但有风险）

提供"一键修复"建议：
- 自动创建缺失目录
- 提供配置修正建议
- 跳过非必要检查项

### 2.3 部署流程模块

#### 2.3.1 部署步骤

1. **环境选择**
   - 选择要部署的环境
   - 显示环境详细信息
   - 自检状态展示

2. **Maven 打包**
   - 显示打包命令
   - 实时输出打包日志
   - 进度条展示
   - 打包结果统计

3. **文件上传**
   - 显示上传文件列表
   - 每个文件的上传进度
   - 上传速度显示
   - 上传结果统计

4. **远程重启**
   - 显示重启命令
   - 实时输出重启日志
   - 重启结果确认

#### 2.3.2 进度控制

**总体进度**:
```
[=====>          ] 45% - 正在上传 platform-startup-project.jar
```

**分步进度**:
1. 环境自检: ████████████ 100%
2. Maven 打包: ████████████ 100%
3. 文件上传:   ████░░░░░░░░ 35%
4. 远程重启:   ░░░░░░░░░░░░ 0%

**实时日志**:
```
[2026-02-28 14:30:15] [INFO] 开始执行 Maven 打包
[2026-02-28 14:30:16] [INFO] Building platform-startup-project 1.0.0
[2026-02-28 14:30:45] [INFO] BUILD SUCCESS
[2026-02-28 14:30:46] [INFO] 开始上传 platform-startup-project.jar
[2026-02-28 14:30:48] [INFO] 上传进度: 45% (12.5MB / 27.8MB)
```

#### 2.3.3 操作控制

- **开始部署**: 启动完整部署流程
- **取消**: 取消当前部署任务
- **重试**: 失败后重试当前步骤
- **跳过**: 跳过当前步骤（仅非关键步骤）

### 2.4 日志管理模块

#### 2.4.1 实时日志

- 日志级别过滤（DEBUG/INFO/WARN/ERROR）
- 关键词搜索
- 日志高亮显示
- 自动滚动到最新日志
- 日志导出（TXT/HTML）

#### 2.4.2 历史日志

- 按日期/环境/状态筛选
- 日志文件管理
- 日志清理策略

### 2.5 部署历史模块

#### 2.5.1 历史记录

记录每次部署的：

- 部署时间
- 环境名称
- 操作用户
- 部署状态（成功/失败/取消）
- 部署耗时
- 部署文件列表
- 失败原因（如失败）

#### 2.5.2 历史查询

- 按时间范围查询
- 按环境查询
- 按状态查询
- 查看详细日志

### 2.6 系统设置模块

#### 2.6.1 全局设置

- 默认超时时间
- 日志保留天数
- 备份策略
- 通知设置（部署完成通知）

#### 2.6.2 安全设置

- 工具不实现账号/权限系统，依赖操作系统现有权限
- 避免在日志中输出密码等敏感信息

---

## 3. 用户界面设计 (UI Design)

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  简易发包工具 v2.0                              [─] [□] [×] │
├─────────────────────────────────────────────────────────────┤
│  [环境管理] [部署中心] [历史记录] [系统设置]               │
├───────────────┬─────────────────────────────────────────────┤
│               │                                             │
│  环境列表     │              主工作区                        │
│               │                                             │
│  🟢 开发环境  │  ┌───────────────────────────────────────┐ │
│  🔵 测试环境  │  │                                       │ │
│  🟡 生产环境  │  │         根据左侧菜单动态展示           │ │
│  ➕ 添加环境  │  │                                       │ │
│               │  │                                       │ │
│               │  └───────────────────────────────────────┘ │
│               │                                             │
├───────────────┴─────────────────────────────────────────────┤
│  [状态栏] 当前环境: 开发环境 | 状态: 就绪 | 上次部署: 成功  │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 环境管理界面

```
┌─────────────────────────────────────────────────────────────┐
│  环境配置: 开发环境                           [保存] [删除] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📋 基本信息                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 环境名称:    [开发环境                          ]   │   │
│  │ 环境标识:    [dev                               ]   │   │
│  │ 环境描述:    [本地开发环境                      ]   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  📁 本地环境配置                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 项目根目录:    [D:\javaproject\backcode      ] [浏览]│   │
│  │ JDK 路径:      [C:\Program Files\Java\jdk... ] [浏览]│   │
│  │ Maven 路径:    [D:\Program Files\...\mvn.cmd ] [浏览]│   │
│  │ Settings 路径: [D:\java_tools\...\settings.xml] [浏览]│   │
│  │ 本地仓库:      [D:\m2\repository            ] [浏览]│   │
│  │                                                     │   │
│  │ [x] 使用安静模式  [x] 精简日志  [x] 离线构建        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🖥️ 服务器配置 (2 台)                   [➕ 添加服务器]    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📌 服务器 1: 主服务器                               │   │
│  │ 地址: 192.168.8.26:22221 | 用户: omp               │   │
│  │ 目录: /home/omp/shanguotou/jar/                    │   │
│  │ 重启脚本: /home/omp/.../restart_jar_dev.sh          │   │
│  │ [x] 启用重启  [ ] 使用 sudo                         │   │
│  │ 状态: ✅ 可用                          [编辑] [删除] │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 📌 服务器 2: 备份服务器                             │   │
│  │ 地址: 192.168.8.27:22221 | 用户: omp               │   │
│  │ 目录: /home/omp/shanguotou/jar/                    │   │
│  │ 重启脚本: /home/omp/.../restart_jar_dev.sh          │   │
│  │ [x] 启用重启  [ ] 使用 sudo                         │   │
│  │ 状态: ⚠️ 未检测                        [编辑] [删除] │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  🏷️ 改名规则配置                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 改名规则: [{jar_name}.jar            ] [预览]       │   │
│  │ 可用变量: {jar_name} {env} {timestamp} {date}       │   │
│  │                                                     │   │
│  │ 预览示例:                                           │   │
│  │   platform-startup-project.jar                     │   │
│  │   → platform-startup-project.jar                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  📦 目标文件 (3 个)                           [➕ 添加文件] │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ☑ platform-startup-project.jar      [默认选中]     │   │
│  │   startup\platform-startup-project\target\...      │   │
│  │                                                     │   │
│  │ ☑ platform-startup-system.jar       [默认选中]     │   │
│  │   startup\platform-startup-system\target\...       │   │
│  │                                                     │   │
│  │ ☐ platform-startup-customer.jar     [默认不选]     │   │
│  │   startup\platform-startup-customer\target\...     │   │
│  │                                                     │   │
│  │ [编辑] [删除] [上移] [下移]                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [🔍 自检环境]  [📋 导入配置]  [📤 导出配置]              │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 部署中心界面

```
┌─────────────────────────────────────────────────────────────┐
│  部署中心                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  选择环境: [开发环境 ▼]                     [🔄 刷新状态]   │
│                                                             │
│  自检状态:                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ✅ 项目根目录存在                                   │   │
│  │ ✅ pom.xml 存在                                     │   │
│  │ ✅ JDK 环境正常 (1.8.0_202)                        │   │
│  │ ✅ Maven 可用 (3.9.12)                             │   │
│  │ ✅ 服务器 192.168.8.26 连接正常                    │   │
│  │ ✅ 远程目录可写                                     │   │
│  │ ✅ 重启脚本可执行                                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  📦 选择要部署的文件:                      [全选] [全不选] │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ☑ platform-startup-project.jar                     │   │
│  │   → 远程文件名: platform-startup-project.jar       │   │
│  │                                                     │   │
│  │ ☑ platform-startup-system.jar                      │   │
│  │   → 远程文件名: platform-startup-system.jar        │   │
│  │                                                     │   │
│  │ ☐ platform-startup-customer.jar                    │   │
│  │   → 远程文件名: platform-startup-customer.jar      │   │
│  │                                                     │   │
│  │ 改名规则: {jar_name}.jar                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  部署进度:                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │  总进度: [████████░░░░░░░░░░░░] 40%                │   │
│  │                                                     │   │
│  │  ✅ 环境自检     ████████████ 100%                  │   │
│  │  ✅ Maven 打包   ████████████ 100%                  │   │
│  │  🔄 文件上传     ████░░░░░░░░ 35%                   │   │
│  │     └─ platform-startup-project.jar: 45%           │   │
│  │     └─ platform-startup-system.jar: 等待中         │   │
│  │  ⏸️ 远程重启     ░░░░░░░░░░░░ 0%                    │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [▶️ 开始部署]  [⏸️ 暂停]  [⏹️ 取消]  [🔄 重试]          │
│                                                             │
│  实时日志:                                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ [INFO] 2026-02-28 14:30:15 开始部署...             │   │
│  │ [INFO] 2026-02-28 14:30:16 Maven 打包成功          │   │
│  │ [INFO] 2026-02-28 14:30:17 开始上传文件...         │   │
│  │ [INFO] 2026-02-28 14:30:18 上传进度: 35%           │   │
│  │ [WARN] 2026-02-28 14:30:19 网络延迟较高            │   │
│  │                                                     │   │
│  │ [全部] [INFO] [WARN] [ERROR]           [导出日志]  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 3.4 历史记录界面

```
┌─────────────────────────────────────────────────────────────┐
│  部署历史                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  筛选: [全部环境 ▼] [全部状态 ▼] [最近7天 ▼]    [🔍 搜索] │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 时间             环境      状态    耗时    操作      │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 2026-02-28       开发环境  ✅ 成功  2分15秒 [详情]  │   │
│  │ 14:30:15                                             │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 2026-02-28       测试环境  ❌ 失败  1分30秒 [详情]  │   │
│  │ 12:15:30                       失败原因: SSH连接超时 │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ 2026-02-27       生产环境  ✅ 成功  3分45秒 [详情]  │   │
│  │ 18:20:10                                             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [上一页] 1 / 5 [下一页]                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 技术实现方案

### 4.1 技术栈

**前端**:
- 框架: Vue 3
- UI 组件库: shadcn-vue
- 样式: Tailwind CSS
- 状态管理: Pinia
- 图标: 基于 shadcn-vue 推荐图标库（如 Lucide）
- 构建工具: Vite

**后端**:
- 框架: Wails v2
- 语言: Go 1.21+
- SSH 库: golang.org/x/crypto/ssh
- SFTP 库: github.com/pkg/sftp
- 配置管理: Viper
- 日志: logrus

**数据存储**:
- 配置文件: JSON / YAML
- 历史记录: SQLite / BoltDB

### 4.2 项目结构

```
deploy-tool-desktop/
├── build/                    # 构建配置
│   └── appicon.png
├── frontend/                 # 前端代码
│   ├── src/
│   │   ├── components/       # 组件
│   │   │   ├── Environment/  # 环境管理组件
│   │   │   ├── Deploy/       # 部署中心组件
│   │   │   ├── History/      # 历史记录组件
│   │   │   └── Settings/     # 系统设置组件
│   │   ├── views/            # 页面视图
│   │   ├── stores/           # 状态管理
│   │   ├── utils/            # 工具函数
│   │   └── App.vue
│   ├── package.json
│   └── vite.config.ts
├── internal/                 # Go 内部包
│   ├── config/               # 配置管理
│   ├── deploy/               # 部署逻辑
│   │   ├── maven.go          # Maven 打包
│   │   ├── ssh.go            # SSH 连接
│   │   ├── sftp.go           # 文件上传
│   │   └── executor.go       # 执行器
│   ├── models/               # 数据模型
│   └── utils/                # 工具函数
├── app.go                    # Wails 应用入口
├── main.go                   # 主程序
└── wails.json                # Wails 配置
```

### 4.3 核心功能实现

#### 4.3.1 配置管理

```go
// internal/config/config.go
type Environment struct {
    ID          string            `json:"id"`
    Name        string            `json:"name"`
    Local       LocalConfig       `json:"local"`
    Servers     []ServerConfig    `json:"servers"`
    Targets     []TargetConfig    `json:"targets"`
    CreatedAt   time.Time         `json:"created_at"`
    UpdatedAt   time.Time         `json:"updated_at"`
}

type LocalConfig struct {
    ProjectRoot    string   `json:"project_root"`
    JavaHome       string   `json:"java_home"`
    MavenCmd       string   `json:"maven_cmd"`
    MavenSettings  string   `json:"maven_settings"`
    MavenRepo      string   `json:"maven_repo"`
    MavenParams    []string `json:"maven_params"`
    QuietMode      bool     `json:"quiet_mode"`
    CompactLog     bool     `json:"compact_log"`
    OfflineMode    bool     `json:"offline_mode"`
}

type ServerConfig struct {
    Host         string `json:"host"`
    Port         int    `json:"port"`
    Username     string `json:"username"`
    Password     string `json:"password"` // 加密存储
    RemoteDir    string `json:"remote_dir"`
    RestartScript string `json:"restart_script"`
    EnableRestart bool  `json:"enable_restart"`
    UseSudo      bool   `json:"use_sudo"`
}
```

#### 4.3.2 部署流程

```go
// internal/deploy/executor.go
type DeployExecutor struct {
    env      *config.Environment
    progress chan Progress
    logs     chan LogEntry
}

func (e *DeployExecutor) Execute() error {
    // 1. 环境自检
    if err := e.checkEnvironment(); err != nil {
        return err
    }
    
    // 2. Maven 打包
    if err := e.mavenBuild(); err != nil {
        return err
    }
    
    // 3. 文件上传
    if err := e.uploadFiles(); err != nil {
        return err
    }
    
    // 4. 远程重启
    if err := e.restartServices(); err != nil {
        return err
    }
    
    return nil
}
```

#### 4.3.3 前端状态管理

```typescript
// frontend/src/stores/deploy.ts
import { defineStore } from 'pinia'

export const useDeployStore = defineStore('deploy', {
  state: () => ({
    currentEnvironment: null,
    deployProgress: {
      total: 0,
      steps: []
    },
    logs: [],
    isDeploying: false
  }),
  
  actions: {
    async startDeploy(envId: string) {
      this.isDeploying = true
      // 调用后端 API
      await window.go.main.App.StartDeploy(envId)
    },
    
    updateProgress(progress: Progress) {
      this.deployProgress = progress
    },
    
    addLog(log: LogEntry) {
      this.logs.push(log)
    }
  }
})
```

---

## 5. 交互流程设计

### 5.1 首次使用流程

```
用户打开应用
    ↓
检测是否存在配置文件
    ↓
[否] → 显示欢迎页面
    ↓
提供环境模板选择
    ↓
用户选择模板或自定义
    ↓
引导配置本地环境
    ↓
引导配置服务器信息
    ↓
引导配置目标文件
    ↓
执行环境自检
    ↓
保存配置
    ↓
进入主界面
```

### 5.2 日常部署流程

```
用户选择环境
    ↓
自动执行环境自检
    ↓
显示自检结果
    ↓
[通过] → 点击"开始部署"
    ↓
执行 Maven 打包
    ↓
实时显示打包进度和日志
    ↓
打包成功 → 开始上传文件
    ↓
显示每个文件的上传进度
    ↓
上传成功 → 执行远程重启
    ↓
显示重启日志
    ↓
部署完成 → 显示结果摘要
    ↓
保存部署历史
```

### 5.3 异常处理流程

```
部署过程中出现错误
    ↓
暂停当前操作
    ↓
显示错误详情
    ↓
提供处理选项:
  - [重试] → 重新执行当前步骤
  - [跳过] → 跳过当前步骤（非关键步骤）
  - [取消] → 取消部署
  - [查看日志] → 查看详细日志
    ↓
用户选择后继续
```

---

## 6. 非功能需求

### 6.1 性能要求

- 应用在普通办公电脑上保持流畅使用
- 配置保存和界面操作无明显卡顿
- 日志区域在常见使用场景下可正常查看和滚动

### 6.2 安全要求

- 避免在日志中明文显示密码等敏感信息
- SSH 连接超时自动断开

### 6.3 可用性要求

- 支持 Windows 10/11

### 6.4 可维护性

- 模块化设计
- 完善的日志记录
- 错误码规范

---

## 7. 开发计划

### 7.1 阶段一：最小可用版本（MVP）

目标：在尽量短时间内交付一个可用的“单环境、单服务器、一键部署”版本，用于替代现有 Python 脚本的日常使用。

范围：
- 项目框架搭建（Wails + Go + Vue3 + shadcn-vue）
- 单环境配置（本地项目路径、JDK、Maven 基础配置）
- 单服务器配置（主机地址、端口、用户名、密码、部署目录、重启脚本）
- Maven 打包功能（命令行调用、基础日志输出）
- 一键部署流程（自检 → 打包 → 上传 → 重启，直线流程）
- 基础 UI 界面（简单表单 + 基础日志输出区域）

不包含：
- 多环境、多服务器
- 部署历史、系统设置
- 复杂错误处理和 UI 美化

### 7.2 阶段二：多环境与自检完善

目标：从单环境扩展为多环境、多服务器，并增强配置自检和 Jar 选择能力，提高日常使用效率与安全性。

范围：
- 环境管理模块完善
  - 支持多套环境（开发、测试、生产等）
  - 环境模板（Dev/Test/Prod）快速创建
  - 环境复制、删除等基础管理操作
- 多服务器配置支持（同一环境下多台服务器串行部署）
- 配置自检模块完善
  - 本地环境自检（JDK、Maven、pom.xml、目标 Jar 等）
  - 远程环境自检（网络连通、SSH、远程目录、重启脚本等）
- Jar 包选择模块
  - 多 Jar 目标文件配置
  - 默认选中逻辑、记住上次选择

### 7.3 阶段三：日志与历史记录

目标：让部署过程可回溯、可排查，支持按环境、时间维度查看历史记录与日志。

范围：
- 实时日志模块完善
  - 日志级别过滤（INFO/WARN/ERROR 等）
  - 关键词搜索、高亮显示
  - 日志导出（TXT/HTML）
- 部署历史模块
  - 记录每次部署的时间、环境、状态、耗时、失败原因等
  - 按时间、环境、状态查询历史
  - 历史记录详情查看（关联日志）
- 系统设置中的日志保留策略（保留天数等）

### 7.4 阶段四：体验优化与发布

目标：在功能基本稳定的基础上，提升整体用户体验和界面美观度，并完成发布前的收尾工作。

范围：
- 前端 UI 与交互优化
  - 统一配色与布局
  - 状态提示与图标优化
  - 表单校验与错误提示优化
- 异常处理优化
  - 统一错误提示风格
  - 常见错误（网络、配置、权限）给出清晰说明
- 测试与修复
  - 功能测试、回归测试
  - 修复发现的问题
- 打包与发布
  - Windows 安装包构建
  - 使用说明文档完善
  - 小范围用户试用与反馈收集

---

## 8. 风险评估

### 8.1 技术风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| Wails 框架稳定性 | 高 | 低 | 关注官方更新，及时升级 |
| SSH 连接兼容性 | 中 | 中 | 支持多种认证方式 |
| 大文件上传性能 | 中 | 中 | 控制并发、优化上传策略 |

### 8.2 业务风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|---------|
| 用户配置错误 | 高 | 高 | 完善自检，提供修复建议 |
| 网络不稳定 | 中 | 中 | 重试机制，友好错误提示 |
| 服务器权限问题 | 高 | 中 | 详细错误提示，权限检查 |

---

## 9. 附录

### 9.1 配置文件示例

```json
{
  "version": "2.0",
  "environments": [
    {
      "id": "env_dev_001",
      "name": "开发环境",
      "code": "dev",
      "description": "本地开发环境",
      "local": {
        "project_root": "D:\\javaproject\\backcode",
        "java_home": "C:\\Program Files\\Java\\jdk1.8.0_202",
        "maven_cmd": "D:\\Program Files\\JetBrains\\IntelliJ IDEA 2025.3.3\\plugins\\maven\\lib\\maven3\\bin\\mvn.cmd",
        "maven_settings": "D:\\java_tools\\apache-maven-3.9.12\\conf\\settings_sgt0903.xml",
        "maven_repo": "D:\\m2\\repository",
        "quiet_mode": true,
        "compact_log": true,
        "offline_mode": true
      },
      "servers": [
        {
          "id": "server_001",
          "name": "主服务器",
          "host": "192.168.8.26",
          "port": 22221,
          "username": "omp",
          "password": "your_password_here",
          "remote_dir": "/home/omp/shanguotou/jar/",
          "restart_script": "/home/omp/shanguotou/jar/restart_jar_dev.sh",
          "enable_restart": true,
          "use_sudo": false
        }
      ],
      "rename_rule": "{jar_name}.jar",
      "targets": [
        {
          "id": "target_001",
          "jar_path": "startup\\platform-startup-project\\target\\platform-startup-project.jar",
          "remote_name": "",
          "default_selected": true
        },
        {
          "id": "target_002",
          "jar_path": "startup\\platform-startup-system\\target\\platform-startup-system.jar",
          "remote_name": "",
          "default_selected": true
        },
        {
          "id": "target_003",
          "jar_path": "startup\\platform-startup-customer\\target\\platform-startup-customer.jar",
          "remote_name": "",
          "default_selected": false
        }
      ],
      "created_at": "2026-02-28T10:00:00Z",
      "updated_at": "2026-02-28T14:30:00Z"
    },
    {
      "id": "env_test_001",
      "name": "测试环境",
      "code": "test",
      "description": "测试服务器环境",
      "local": {
        "project_root": "D:\\javaproject\\backcode",
        "java_home": "C:\\Program Files\\Java\\jdk1.8.0_202",
        "maven_cmd": "D:\\Program Files\\JetBrains\\IntelliJ IDEA 2025.3.3\\plugins\\maven\\lib\\maven3\\bin\\mvn.cmd",
        "maven_settings": "D:\\java_tools\\apache-maven-3.9.12\\conf\\settings_sgt0903.xml",
        "maven_repo": "D:\\m2\\repository",
        "quiet_mode": false,
        "compact_log": false,
        "offline_mode": false
      },
      "servers": [
        {
          "id": "server_002",
          "name": "测试服务器1",
          "host": "192.168.9.10",
          "port": 22221,
          "username": "omp",
          "password": "your_password_here",
          "remote_dir": "/home/omp/shanguotou/jar/",
          "restart_script": "/home/omp/shanguotou/jar/restart_jar_test.sh",
          "enable_restart": true,
          "use_sudo": false
        },
        {
          "id": "server_003",
          "name": "测试服务器2",
          "host": "192.168.9.11",
          "port": 22221,
          "username": "omp",
          "password": "your_password_here",
          "remote_dir": "/home/omp/shanguotou/jar/",
          "restart_script": "/home/omp/shanguotou/jar/restart_jar_test.sh",
          "enable_restart": true,
          "use_sudo": false
        }
      ],
      "rename_rule": "{jar_name}_test.jar",
      "targets": [
        {
          "id": "target_001",
          "jar_path": "startup\\platform-startup-project\\target\\platform-startup-project.jar",
          "remote_name": "",
          "default_selected": true
        },
        {
          "id": "target_002",
          "jar_path": "startup\\platform-startup-system\\target\\platform-startup-system.jar",
          "remote_name": "",
          "default_selected": true
        }
      ],
      "created_at": "2026-02-28T10:00:00Z",
      "updated_at": "2026-02-28T14:30:00Z"
    }
  ],
  "settings": {
    "timeout": 600,
    "log_retention_days": 30,
    "enable_notification": true,
    "remember_last_selection": true
  }
}
```

### 9.2 错误码定义

| 错误码 | 说明 | 处理建议 |
|--------|------|---------|
| E001 | 项目根目录不存在 | 检查路径配置 |
| E002 | pom.xml 不存在 | 确认项目结构 |
| E003 | JDK 未安装或配置错误 | 安装 JDK 或配置 JAVA_HOME |
| E004 | Maven 未安装或配置错误 | 安装 Maven 或配置路径 |
| E005 | SSH 连接失败 | 检查网络和服务器状态 |
| E006 | SFTP 上传失败 | 检查目录权限 |
| E007 | 远程脚本执行失败 | 检查脚本权限和内容 |
| E008 | Maven 打包失败 | 查看详细日志 |

---

## 10. 改名规则与 Jar 包选择详细说明

### 10.1 改名规则配置

#### 10.1.1 规则语法

改名规则支持以下变量：

| 变量 | 说明 | 示例值 |
|------|------|--------|
| `{jar_name}` | 原始 Jar 包名（不含扩展名） | `platform-startup-project` |
| `{env}` | 环境标识 | `dev`, `test`, `prod` |
| `{timestamp}` | 时间戳（精确到秒） | `20260228143025` |
| `{date}` | 日期（YYYYMMDD） | `20260228` |
| `{time}` | 时间（HHMMSS） | `143025` |
| `{version}` | 版本号（从 pom.xml 读取） | `1.0.0` |

#### 10.1.2 规则示例

**开发环境**（不改名）:
```
规则: {jar_name}.jar
原始: platform-startup-project.jar
结果: platform-startup-project.jar
```

**测试环境**（添加环境标识）:
```
规则: {jar_name}_{env}.jar
原始: platform-startup-project.jar
结果: platform-startup-project_test.jar
```

**生产环境**（添加时间戳）:
```
规则: {jar_name}_{timestamp}.jar
原始: platform-startup-project.jar
结果: platform-startup-project_20260228143025.jar
```

**完整版本**（多变量组合）:
```
规则: {jar_name}_v{version}_{date}.jar
原始: platform-startup-project.jar
结果: platform-startup-project_v1.0.0_20260228.jar
```

#### 10.1.3 规则验证

在配置改名规则时，提供实时预览：

```
改名规则: [{jar_name}_{env}.jar        ] [预览]

预览示例:
  platform-startup-project.jar
  → platform-startup-project_dev.jar

  platform-startup-system.jar
  → platform-startup-system_dev.jar
```

### 10.2 Jar 包选择机制

#### 10.2.1 默认选中逻辑

每个目标文件可配置 `default_selected` 字段：

```json
{
  "jar_path": "startup\\platform-startup-project\\target\\platform-startup-project.jar",
  "default_selected": true  // 默认选中
}
```

部署时：
- 首次打开：按照 `default_selected` 配置显示勾选状态
- 后续打开：可选择记住上次的选择（在系统设置中配置）

#### 10.2.2 选择界面交互

```
┌─────────────────────────────────────────────────────────┐
│ 📦 选择要部署的文件:                    [全选] [全不选] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ☑ platform-startup-project.jar                         │
│   本地路径: startup\platform-startup-project\target\   │
│   远程文件名: platform-startup-project_dev.jar         │
│   文件大小: 27.8 MB                                     │
│                                                         │
│ ☑ platform-startup-system.jar                          │
│   本地路径: startup\platform-startup-system\target\    │
│   远程文件名: platform-startup-system_dev.jar          │
│   文件大小: 25.3 MB                                     │
│                                                         │
│ ☐ platform-startup-customer.jar                        │
│   本地路径: startup\platform-startup-customer\target\  │
│   远程文件名: platform-startup-customer_dev.jar        │
│   文件大小: 22.1 MB                                     │
│                                                         │
│ ────────────────────────────────────────────────────── │
│ 已选择: 2 个文件 | 总大小: 53.1 MB                      │
│                                                         │
│ [使用上次选择] [恢复默认]                               │
└─────────────────────────────────────────────────────────┘
```

#### 10.2.3 批量操作

- **全选**: 选中所有 Jar 包
- **全不选**: 取消选中所有 Jar 包
- **使用上次选择**: 恢复上次部署时的选择（需开启设置）
- **恢复默认**: 恢复到 `default_selected` 配置的状态

#### 10.2.4 选择状态持久化

在系统设置中可配置：

```json
{
  "settings": {
    "remember_last_selection": true,  // 记住上次选择
    "last_selection": {
      "env_dev_001": ["target_001", "target_002"],
      "env_test_001": ["target_001"]
    }
  }
}
```

### 10.3 多服务器部署逻辑

#### 10.3.1 部署顺序

当环境配置了多个服务器时，部署顺序如下：

```
1. Maven 打包（本地，仅执行一次）
     ↓
2. 上传到服务器 1
     ↓
3. 上传到服务器 2
     ↓
4. ...
     ↓
5. 重启服务器 1（如启用）
     ↓
6. 重启服务器 2（如启用）
     ↓
7. 完成
```

#### 10.3.2 失败处理

- **上传失败**: 跳过该服务器，继续上传到下一个服务器
- **重启失败**: 记录错误日志，不影响其他服务器
- **部分成功**: 显示成功/失败统计，提供重试选项

#### 10.3.3 进度展示

```
部署进度:
┌─────────────────────────────────────────────────────┐
│ 总进度: [████████░░░░░░░░░░░░] 40%                 │
│                                                     │
│ ✅ 环境自检     ████████████ 100%                  │
│ ✅ Maven 打包   ████████████ 100%                  │
│ 🔄 文件上传     ████░░░░░░░░ 35%                   │
│    ├─ 服务器 1: ✅ 完成                            │
│    └─ 服务器 2: 🔄 上传中 45%                      │
│ ⏸️ 远程重启     ░░░░░░░░░░░░ 0%                    │
│    ├─ 服务器 1: ⏸️ 等待中                          │
│    └─ 服务器 2: ⏸️ 等待中                          │
└─────────────────────────────────────────────────────┘
```

---

## 11. 总结

本需求文档详细描述了简易发包工具桌面版的功能需求、UI 设计、技术方案和开发计划。通过 Wails + Go + Vue.js 的技术栈，将现有的 Python 脚本升级为 Windows 桌面应用，提供更好的用户体验和更强大的功能。

核心亮点：
- ✅ 可视化配置管理，支持多套环境
- ✅ 实时进度展示，清晰掌握部署状态
- ✅ 完善的自检机制，提前发现问题
- ✅ 详细的日志记录，便于问题排查
- ✅ 历史记录管理，追溯部署历史

下一步工作：
1. 技术选型确认
2. 原型设计
3. 开发环境搭建
4. 分阶段实施开发
